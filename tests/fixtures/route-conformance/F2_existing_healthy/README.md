# calc-tiny

Two integer math components. Layout:

- `modules/add.txt` — addition contract
- `modules/mul.txt` — multiplication contract
- `CHANGELOG.md` — history

No build system. Contracts are the code.
