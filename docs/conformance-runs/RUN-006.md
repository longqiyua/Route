# RUN-006 — P8 Cross-Session Continuity (F6_abandoned)

- run_id: RUN-006
- timestamp: 2026-08-16 13:02–13:03 HKT (log: 05:02 UTC)
- fixture/project_id: F6_abandoned
- project_initial_state: note-taking app doc + real Route state, NO previous
  chat context (abandoned project)
- model_if_known: GLM (Trae) — LIMITATION: both "sessions" ran in the same
  model instance family within one harness; a truly fresh-model resume is
  NOT_RUN (see conformance.md §5)
- boom_available: NO
- session_id: 01M04F5EH0C5 (`implement-tag-notes-feature`, session 1,
  Succeeded), 01M04F6WEYWM (`session2-complete-tag-notes-checkbox`, session
  2, Succeeded)
- execution_provenance: engine audit `task show 01M04F6WEYWM` (re-read this
  session) + app.md diff history

## SESSION 1 (take over abandoned project)

1. Takeover from Route state + project files only — OBSERVED.
2. Meaningful task executed (tag-notes feature per app.md roadmap) —
   OBSERVED.
3. Decision recorded in `app.md` §Tags design (flat `tags: set<string>`;
   hierarchical rejected + rationale; OPEN question deferred to
   search-notes task) — OBSERVED (commit `session1: tag design section +
   open question deferred`).

## SESSION 2 (continuation)

1. No previous conversation available; relied on project + Route state —
   OBSERVED (within same-harness limitation noted above).
2. Recovered: project identity, current task, prior decision (flat tags),
   open question, verified-vs-unverified split — OBSERVED.
3. Executed next task (mark tag-notes done in app.md) WITHOUT
   re-reconstructing session 1 — OBSERVED.
4. Evidence: engine `system/check_pass` (evidence 01M04F79BMKX, argv
   Select-String `[x] tag notes`, exit 0) — OBSERVED.

## Shared-state observation (P4 partial)

Durable facts persisted through Route state + project files; session 2 read
decision + lineage without manager-message relay — OBSERVED. `route memory
show` returned "No project memory available" (memory was recorded via
`learn record`, `memory refresh` not run) — noted, not a failure.

## final_state

App doc updated, both sessions Succeeded with system evidence, lineage
intact.

## Honest limitation

CROSS_SESSION_CONTINUITY = PARTIAL: state semantics proven; fresh-model /
fresh-harness resume NOT_RUN this round.
