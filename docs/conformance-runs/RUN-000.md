# RUN-000 — Environment Verification

- run_id: RUN-000
- timestamp: 2026-08-16 ~12:50 HKT (before fixture runs)
- fixture/project_id: n/a (repo root)
- project_initial_state: Route repo, engine built at `target/release/route.exe`
- model_if_known: GLM (Trae harness)
- harness_if_known: Trae Code (tools: file R/W, shell, subagents)
- harness_capabilities: subagents YES, skills YES
- boom_available: NO (no implementation exists — see docs/boom.md §29)
- execution_provenance: direct shell invocations in this session

## Observed

- `route.exe` present and functional (release build).
- Engine state directory created by `init` in this build: `.route-basic/`
  (sqlite + objects). Protocol-documented `.route/` semantic domains are
  partially written by context/apply features (`F7` shows both trees).
- Windows `route.exe` name collision: bare `route` in a fixture cwd resolves
  to the OS network tool (`System32\route.exe`), NOT the engine. All runs must
  invoke the engine by absolute path. Recorded as environment caveat, not a
  protocol ambiguity.

## Result

OBSERVED (engine available; engine optional — every lifecycle below was driven
by AI protocol discipline, engine used only for state/evidence/recording).
