[package]
name = "route-vibe"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
serde.workspace = true
serde_json.workspace = true
tracing.workspace = true
anyhow.workspace = true
chrono.workspace = true
route-vm = { path = "../route-vm", optional = true }
route-skill = { path = "../route-skill", optional = true }
route-memory = { path = "../route-memory", optional = true }
route-engine = { path = "../route-engine", optional = true }

[dev-dependencies]
tempfile.workspace = true

[features]
default = []
vm = ["route-vm"]
skill = ["route-skill"]
memory = ["route-memory"]
engine = ["route-engine"]
full = ["vm", "skill", "memory", "engine"]