# 2026-08-19 — Route v1.0 beta Release Closure

- **Date**: 2026-08-19
- **Route version**: 1.0.0-beta (display: v1.0 beta)
- **Goal**: Close the first-generation Route public beta: converge the three-branch contract, finalize versioning, verify standalone usability, and prove Route is a real, independent, runnable OSS project on `main`.
- **Trigger**: User release-closure instruction (ROUTE FIRST / MAIN × FRUIT × SANDBOX / self-dogfood without Yuich dependency).
- **Source branch/ref**: `fruit` @ `e30ce43` (Route canonical source at `tool/route/`), `main` @ `0ae87da` (old lineage), `sandbox` @ `81c1faf`.
- **Development intent**: Route must be valuable without Yuich. Yuich is only one advanced Host. This round: no new grand features — only release fixes, versioning, documentation, public-boundary cleanup, and honest evidence.

## What Route understood

- The current integration (`fruit`) hosts Route at `tool/route/`; a standalone `main` view should have Route at repository root.
- Route code has **zero** Yuich imports — ownership audit PASS.
- Version strategy: product release (`1.0.0-beta`) is not the same number as Rust workspace (`1.0.0`), protocol revision, schema revision, or Handle revision.
- Real license is **AGPL-3.0** (main `LICENSE` file); stale metadata (`Apache-2.0` in old `Cargo.toml`, `MIT` in `TOOL.json`) needed correction.

## What changed

- `TOOL.json`: version `0.4.0-beta` → `1.0.0-beta`; license `MIT` → `AGPL-3.0`; degradation_policy made host-neutral (removed Yuich-only wording).
- `packages/route-py/pyproject.toml` + `route/__init__.py`: version `0.4.0-beta` → `1.0.0-beta`.
- `crates/route-cli/src/commands.rs`: removed 3 unused `use route_basic::emergence::*;` imports (self-dogfood friction fix).
- `main` branch: Route canonical source moved from old-lineage tree to repository root (see `2026-08-19-route-fruit-to-main-promotion.md`); README.md / ROUTE.md / THIRD_PARTY_NOTICES.md rewritten for the converged Route.

## What did not change

- Rust workspace version stays `1.0.0`.
- Route core semantics unchanged; no new architecture, no new features.
- Yuich version untouched (`1.0.0-beta`, not a Yuich release).
- No tag / no GitHub Release created.

## AI Worker role

Bounded executor under BranchGate/PathGate/CommandGate/DiffGate/TestGate/ClaimEvidenceGate:
- Made only release-scoped changes (versioning, docs, dead-import cleanup).
- Did not self-promote; promotion decisions recorded separately.

## Evidence

- `cargo test --workspace` — PASS (zero failures across all workspace members).
- `route-basic` — PASS (323 tests).
- CLI sanity — PASS (`--help`, `--version` → `route 1.0.0`).
- `maturin build --release` — PASS (wheel generated; import smoke test PASS).
- Existing-project test — PASS (init → status → commit → log on a non-empty project).
- Route self-dogfood — PASS (dead-import cleanup; `cargo test -p route-cli` green).

## Tests

As above; all REAL_OBSERVED on this machine. No fixture masquerading as real.

## Failures / Recovery

- `route init` inside a TRAE sandbox was blocked (restricted path); recovered by running in a temp project directory. `.route-basic` accidentally created at repo root was removed (it was newly created, git-ignored, no user data).

## Outcome

Route v1.0 beta closure: MAIN_ROUTE_ONLY, ROUTE_STANDALONE PASS, ROUTE_DEPENDS_ON_YUICH NO, WORKSPACE_REGRESSION PASS, ROUTE_BASIC PASS, ROUTE_PY PASS, CLI_SANITY PASS, EXISTING_PROJECT PASS, SELF_DOGFOOD PASS, VERSION 1.0.0-beta.

## Status

CONFIRMED (release closure), pending human review of `main` before push.

## Route learned

- Product release versioning must be decoupled from internal workspace/component versioning.
- Public manifests (`TOOL.json`) can drift from the real license if not audited at release time.
- Cleanest main = repo root Route, host embedding stays under `tool/route/`.

## Unresolved

- Whether to `git tag` / publish GitHub Release after human review (explicitly deferred to the user).
- Optional environment-dependent e2e suites not run on this machine are recorded as NOT_RUN, not silently skipped.

## Related commits

- fruit: `e30ce43`, `bbd0618`; main: local closure commit (pending human review); sandbox: this record.
