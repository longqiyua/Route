# RUN-007 — Partial / Stale Route State Takeover (F7_partial_route)

- run_id: RUN-007
- timestamp: 2026-08-16 ~13:03 HKT (log: 05:03 UTC)
- fixture/project_id: F7_partial_route
- project_initial_state: project + deliberately incomplete Route state
  (`.route/` semantic tree missing `execution/` domain and root
  `protocol.md`; stale context archive present)
- model_if_known: GLM (Trae) / harness: Trae Code / boom_available: NO
- session_id: none started (takeover/assessment round — observation-only,
  no mutation task)
- execution_provenance: directory state (still observable now) + prior
  session tool calls

## observed_actions

1. Detected Route state present but INCOMPLETE (missing semantic files) —
   OBSERVED.
2. Did NOT invent missing history: gaps marked UNKNOWN/STALE rather than
   reconstructed by assumption — OBSERVED (P12 honesty).
3. Treated existing state as append-only: no deletion/rewrite of stale
   archive — OBSERVED.
4. `route init` re-anchored engine state (`.route-basic/` created) without
   touching user files — OBSERVED (baseline commit `baseline` +3).

## KEY FINDING — integrity check gap (AMB-003)

After deleting `.route/execution/` and root `protocol.md` (fixture design),
`route check` still reported **Status: OK**. Integrity verification does not
cover the protocol-documented semantic domains under `.route/` (it validates
the engine's own `.route-basic` ledger instead). Recorded as ambiguity with
evidence; not standardized away.

## final_state

Stale state preserved + documented; takeover resumed cleanly on top.

## anti_check_results

No fabricated history; no destructive "repair" of state — PASS.
